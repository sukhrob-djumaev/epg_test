import 'package:flutter/material.dart';

class AppAsyncDataEmpty extends StatelessWidget {
  const AppAsyncDataEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Nothing to show'));
  }
}
