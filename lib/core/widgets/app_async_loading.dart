import 'package:flutter/material.dart';

class AppAsyncLoading extends StatelessWidget {
  const AppAsyncLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
